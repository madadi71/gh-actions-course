name: 05 - 2 - Event Filter and Activity Types

on:
 pull_request:
     types:
         - closed
     branches:
         - main


jobs:
  echo:
    runs-on: unbuntu-latest
    steps:
        - run: echo "Running whenever a pull request is closed."