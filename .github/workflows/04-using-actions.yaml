name: 04 - Using Actions

on: push

jobs:
  build:
      runs-on: ubuntu-latest 
      steps:
          - name: Checkout Coede
            uses: actions/checkout@v3
          - name: Printong Folders
            run: ls